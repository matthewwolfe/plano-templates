import { create } from 'zustand';

export interface {{pascalCase name}}Store {

}

const use{{pascalCase name}}Store = create<{{pascalCase name}}Store>()(
  (set) => ({

  }),
);

export { use{{pascalCase name}}Store };
